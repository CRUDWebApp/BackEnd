apiVersion: apps/v1
kind: Deployment
metadata:
  name: crud-app

spec:
  replicas: 1

  selector:
    matchLabels:
      app: crud-app

  template:
    metadata:
      labels:
        app: crud-app

    spec:

      imagePullSecrets:
      - name: ecr-secret

      containers:

      - name: api

        image: {{image}}

        ports:
        - containerPort: 3000
        
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: backend-secret
              key: DATABASE_URL
